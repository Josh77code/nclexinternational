-- ============================================
-- QUICK START: Add Student to Category
-- ============================================
-- Copy ONE of these queries and run it in Supabase SQL editor
-- Replace 'student-email@example.com' with the actual email
-- ============================================

-- Add student to Starter (Beginner) category
UPDATE users 
SET student_grade = 'starter' 
WHERE email = 'student-email@example.com' 
AND role = 'student';

-- Add student to Mid (Intermediate) category
UPDATE users 
SET student_grade = 'mid' 
WHERE email = 'student-email@example.com' 
AND role = 'student';

-- Add student to Higher (Advanced) category
UPDATE users 
SET student_grade = 'higher' 
WHERE email = 'student-email@example.com' 
AND role = 'student';



-- QUICK START: Add Student to Category
-- ============================================
-- Copy ONE of these queries and run it in Supabase SQL editor
-- Replace 'student-email@example.com' with the actual email
-- ============================================

-- Add student to Starter (Beginner) category
UPDATE users 
SET student_grade = 'starter' 
WHERE email = 'student-email@example.com' 
AND role = 'student';

-- Add student to Mid (Intermediate) category
UPDATE users 
SET student_grade = 'mid' 
WHERE email = 'student-email@example.com' 
AND role = 'student';

-- Add student to Higher (Advanced) category
UPDATE users 
SET student_grade = 'higher' 
WHERE email = 'student-email@example.com' 
AND role = 'student';




