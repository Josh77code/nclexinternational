import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'NCLEX Keys International Academy',
  description: 'Professional Healthcare Education - NCLEX Preparation & Training',
  generator: 'nclexkeysinternational.com',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} bg-white`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
